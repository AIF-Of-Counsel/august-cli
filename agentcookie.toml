# agentcookie.toml: secrets-bus adoption manifest v2
# See docs/spec-agentcookie-secrets-bus-v2-adoption.md
schema_version = 2
name = "august-pp-cli"
display_name = "August Public"
description = "Programmatic access to August. Authenticate every request with a Clerk API key: `Authorization: Bearer ak_...`."
project_kind = "cli"

[secrets.file]
path = "~/.config/august-pp-cli/config.toml"

[sync]
default = false

[sync.keys]
AUGUST_AUTHORIZATION = true
